//Personal Function
//Bria Shaw
//March 21, 2014

var weeklyPay = prompt("enter weekly hours, pay per hour, overtime hours, overtime pay", [40, 10, 4, 15]);
function weeklyPay = function(weeklyHours * payPerHour + overtimeHours * overtimePay; 
	)
	var weeklyPay = weeklyHours * payPerHour + overtimeHours * overtimePay;
	return weeklyPay;
}

var a = weeklyPay(40,10,4,15);
console.log(a);